package messages;

/**
 * Holds messages that are plain strings.
 */
public interface StringMessages {
    /**
     * The message that is exchanged between car and human when the car arrives at the human position.
     */
    String CAR_ARRIVED = "Car arrived";
}
